# 双机开发同步台账

本文档是 macOS 与 Windows 两台开发机器之间同步**当前开发进度**的唯一台账。

文档职责边界：

- 当前进度、待修复 Bug、待优化项、待开发项、验证结果和机器交接记录写在这里。
- 已完成并纳入版本的变更写入 `CHANGELOG.md`，本文档只保留当前分支的简要状态，不复制完整版本历史。
- 安装、配置和使用方法写入 `README.md`，本文档不重复维护操作手册。
- 密钥、Cookie、`.env` 内容、本机绝对路径和其他敏感信息禁止写入本文档。

## 当前开发基线

| 项目 | 当前状态 |
| --- | --- |
| 最后更新 | 2026-07-30 |
| 最后操作机器 | macOS |
| 开发阶段 | v1.2 开发中，跨平台环境自检已完成，等待 Windows 联合验收 |
| 当前分支 | `codex/multimodal-video-analysis` |
| 功能基线提交 | `26eee2d`（新增跨平台环境自检入口） |
| 远端同步 | 已与 `origin/codex/multimodal-video-analysis` 对齐 |
| 相对 `main` | 领先 6 个提交，其中 5 个功能提交、1 个同步台账提交 |
| 工作区 | 无未提交代码改动；`.DS_Store` 已加入忽略规则 |
| 最近验证 | macOS 58 项单元测试全部通过；Docker 默认自检 8 项通过，视觉自检 11 项通过 |

> 切换机器后，确认当前分支包含“功能基线提交”。同步台账可能位于该功能提交之后，不要求 `HEAD`
> 与功能基线提交号完全相同。

## 正在进行

当前没有正在编写但尚未提交的代码。

下一步由 Windows 机器拉取 `codex/multimodal-video-analysis`，先执行单元测试和两条 `--doctor`
命令，再进行快速解析与多模态解析的真实视频验收。

## 待修复 Bug

暂无已确认且未修复的 Bug。

发现问题时按下面格式登记；未经复现的问题先放入“风险与待确认事项”，不要直接标记为 Bug。

| 编号 | 问题 | 复现条件 | 影响 | 优先级 | 状态 | 处理提交 |
| --- | --- | --- | --- | --- | --- | --- |
| - | 暂无 | - | - | - | - | - |

## 待优化项

| 编号 | 优化点 | 验收标准 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| OPT-001 | 补齐并统一 Windows 与 macOS 的多模态运行说明 | Windows 实测命令、Ollama 连通方式和平台限制与 `README.md` 描述一致，不保留互相矛盾的说明 | 中 | 等待 Windows 实测 |
| OPT-002 | 提供跨平台环境自检和统一验收入口 | `--doctor` 可安全检查运行条件，文本和 JSON 均不泄露敏感配置，Windows 可复用 | 高 | macOS 验证通过，等待 Windows 验收 |

## 待开发与验证项

| 编号 | 类型 | 内容 | 完成标准 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| DEV-001 | 验证 | Windows 执行完整单元测试 | 测试全部通过，并将命令、测试数量和结果写入交接记录 | 高 | 待处理 |
| DEV-002 | 验证 | Windows 验证默认快速解析 | Docker 能完成一次真实视频处理，生成有效的 `transcript.txt`、`summary.md` 和 `meta.json` | 高 | 待处理 |
| DEV-003 | 验证 | Windows 验证多模态解析与宿主机 Ollama 连通 | 使用 `--with-vision` 完成一次真实视频处理，关键帧、视觉上下文、多模态上下文和总结均正常生成 | 高 | 待处理 |
| DEV-004 | 发布 | 确认 v1.2 分支是否合并到 `main` | DEV-001～DEV-003 通过；遗留风险已记录；合并后两台机器切回同一目标分支 | 高 | 被 Windows 验证阻塞 |
| DEV-005 | 开发 | 增加跨平台 `--doctor` 自检 | 无视频地址即可检查依赖、工具、输出权限和模型服务；支持视觉检查、JSON 和正确退出码 | 高 | macOS 已完成 |

## 风险与待确认事项

| 编号 | 内容 | 当前判断 | 确认方式 |
| --- | --- | --- | --- |
| RISK-001 | Windows Docker 容器访问宿主机 Ollama 的行为尚未在本轮验证 | 可能受 Docker Desktop、端口监听或防火墙影响；目前不是已确认 Bug | 执行 DEV-003，并记录失败阶段和错误信息 |
| RISK-002 | 单元测试不覆盖所有真实视频站点、网络、Cookie、FFmpeg 和本地模型行为 | 外部依赖变化仍可能导致端到端失败 | 分别执行 DEV-002、DEV-003 |
| RISK-003 | 当前功能分支相对 `main` 改动较大 | 合并前需要保留完整验证证据 | Windows 验证完成后检查 Git diff、测试结果和 `CHANGELOG.md` |

## 最近完成

- 当前功能分支包含 5 个尚未合入 `main` 的功能提交：
  - `2e93b84`：优化通用视频多模态解析与本地模型总结。
  - `6d5705b`：修复多模态解析的缓存与失败边界。
  - `c20fe40`：修复多模态解析的终审边界。
  - `81a5df0`：修复长视频分段摘要并补充画面阅读文档。
  - `26eee2d`：新增跨平台环境自检入口。
- macOS 已执行：

  ```bash
  python3 -m unittest discover -s tests -v
  ```

  结果：`Ran 58 tests`，`OK`。
- 新增 `--doctor` 环境自检入口及 12 项测试，功能提交为 `26eee2d`。
- Docker 默认自检：8 项通过、0 项警告、0 项失败。
- Docker 视觉 JSON 自检：11 项通过、0 项警告、0 项失败；总结和视觉模型均可连接且目标模型存在。
- 功能明细以 `CHANGELOG.md` 的“v1.2（开发中）”为准，避免在两份文档中重复维护。

## 双机切换流程

### 接手开发前

1. 确认另一台机器已经提交并推送交接内容。
2. 拉取远端信息并切换到台账记录的分支：

   ```bash
   git fetch origin
   git switch codex/multimodal-video-analysis
   git pull --ff-only
   git status
   ```

3. 确认当前分支包含功能基线提交：

   ```bash
   git merge-base --is-ancestor 26eee2d HEAD
   ```

   命令退出码为 0 表示当前分支包含该功能基线。
4. 运行与本次任务相关的测试，再开始修改。
5. 不复制 `.env`、Cookie、模型缓存、输出视频或总结产物到 Git。

### 结束开发并交接

1. 完成测试，并记录实际执行命令和结果。
2. 更新本文档中的当前基线、任务状态、风险和交接记录。
3. 将代码与本文档放在同一个有明确含义的提交中；如果只更新交接状态，可单独提交文档。
4. 推送当前分支，并确认工作区没有遗漏的源代码改动。
5. 下一台机器必须先拉取，不允许在旧提交上继续开发后强行覆盖远端。

## 平台差异记录

| 项目 | macOS | Windows | 同步要求 |
| --- | --- | --- | --- |
| Python 测试命令 | `python3 -m unittest discover -s tests -v` | 优先尝试 `py -m unittest discover -s tests -v`；若环境使用 `python`，记录实际命令 | 结果写入交接记录 |
| Docker 命令 | `docker compose ...` | `docker compose ...` | 业务参数保持一致 |
| Ollama | 当前 macOS 验证基线 | 等待验证 Docker Desktop 到宿主机 Ollama 的连通性 | 不把机器专属地址硬编码进代码 |
| 路径 | POSIX 路径 | Windows 宿主机路径；传入容器时仍使用容器路径 | 使用方法只在 `README.md` 维护 |
| 本机文件 | 可能生成 `.DS_Store` | 可能生成编辑器或系统临时文件 | 均不得提交 |

## 交接记录

只追加能够帮助下一台机器继续工作的记录。版本长期历史仍归 `CHANGELOG.md`。

| 日期 | 机器 | 分支 / 提交 | 本次结果 | 验证 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-30 | macOS | `codex/multimodal-video-analysis` / `81a5df0` | 建立双机同步台账；确认当前无未提交代码改动，功能分支领先 `main` 4 个提交 | `python3 -m unittest discover -s tests -v`：46 项通过 | Windows 拉取该分支，依次执行 DEV-001～DEV-003 |
| 2026-07-30 | macOS | `codex/multimodal-video-analysis` / `26eee2d` | 完成跨平台 `--doctor`、安全 JSON 输出、模型探测、12 项新增测试和使用说明 | 58 项单元测试通过；Docker 默认自检 8 项通过；Docker 视觉自检 11 项通过 | Windows 拉取分支，运行单元测试、`--doctor`、`--doctor --with-vision`，再执行 DEV-002～DEV-003 |

## 新增事项模板

新增任务时复制下面一行到对应表格，并分配稳定编号：

```text
| TYPE-编号 | 内容 | 可验证的完成标准 | 优先级 | 待处理 |
```

交接记录应回答四个问题：使用了哪个提交、完成了什么、如何验证、下一台机器接着做什么。
